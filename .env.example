# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=your_secure_password_here
DATABASE_NAME=nestbackend

# Application
NODE_ENV=development

# Auth - generate your own with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change_me_to_a_random_secret

# Custom Orders API key -- generate the same way as JWT_SECRET above. Give
# this value to any third party that needs to call the Custom Orders
# endpoints (GET product types, POST orders) without a user login; they send
# it back as an `X-API-Key` header.
CUSTOM_ORDERS_API_KEY=change_me_to_a_random_secret

# AI - Gemini API key, get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Google Drive backup -- create an OAuth client at
# https://console.cloud.google.com/apis/credentials (Web application type),
# enable the Google Drive API, and add GOOGLE_REDIRECT_URI below as an
# authorized redirect URI on that client.
GOOGLE_CLIENT_ID=your_google_oauth_client_id
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3001/settings/google-drive/callback
# Where the backend sends the browser back to after a Drive connect attempt.
FRONTEND_URL=http://localhost:5173
# How many days of backups to keep, locally and on Drive.
BACKUP_KEEP_DAYS=14
